# ---------- Groq ? main API (free, ultra-fast) ----------
grog.key=YOUR_GROQ_API_KEY

# ---------- OpenRouter ? fallback ----------
openrouter.key=YOUR_OPENROUTER_API_KEY

# ---------- Grok (xAI) ? secondary fallback ----------
grok.key=YOUR_GROK_API_KEY

# ---------- YouTube API ? for video suggestions ----------
youtube.key=YOUR_YOUTUBE_API_KEY

# ---------- Cohere ? backup/supplementary ----------
cohere.key=YOUR_COHERE_API_KEY